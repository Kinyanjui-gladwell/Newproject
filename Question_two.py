"""Return IP addresses that accessed the server the most"""
def count(dictionary):
    keys = dictionary.keys()
    count_list = []
    for key in keys:
        if dictionary.has_keys(key):
            count = count + 1
        count_list.appen

